#!/bin/bash
# Lab 262: Internet Protocols — Static and Dynamic Addresses
# Console-only lab — no CLI commands required during the lab itself.
# All EC2 and Elastic IP operations are performed via the AWS Management Console.

# ── Git: commit and push ────────────────────────────────────────────────────
cd ~/Desktop/AWS-reStart-Journey

git pull origin main --no-rebase

git add Labs/Networking/lab-262-static-dynamic-addresses/

git commit -m "Add Lab 262: Static and Dynamic Addresses (Elastic IP)"

git push origin main
