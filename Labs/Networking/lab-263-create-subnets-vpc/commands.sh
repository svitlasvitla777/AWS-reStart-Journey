#!/usr/bin/env zsh
# Lab 263 — Create Subnets and Allocate IP Addresses in an Amazon VPC
# Git commands only — lab performed entirely in the AWS Management Console

# ── Navigate to repo root ─────────────────────────────────────────────────
cd ~/Desktop/AWS-reStart-Journey

# ── Create lab directory structure ───────────────────────────────────────
mkdir -p Labs/Networking/lab-263-create-subnets-vpc/screenshots

# ── Stage, commit, and push ───────────────────────────────────────────────
git add Labs/Networking/lab-263-create-subnets-vpc/
git commit -m "Add Lab 263: Create Subnets and Allocate IP Addresses in Amazon VPC"
git push origin main
